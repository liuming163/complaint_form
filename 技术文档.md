# 技术文档

## 一、文档目的

帮助接手或部署本项目的开发/运维同学，在 10 分钟内建立全局认识，包括：项目做什么、用什么技术栈、目录长什么样、启动需要哪些组件、有哪些 HTTP 接口、如何部署、出问题先看哪。

业务侧使用说明请看 `业务操作使用说明.md`，本文不重复。

---

## 二、项目简介

- **项目名称**：投诉处理系统
- **目标**：在 Web 端集中管理投诉账号、被代理人资料、作品资料；接收投诉表单后通过浏览器自动化（UC）或 API 调用（百度、夸克、微博）批量提交到对应投诉平台。
- **运行形态**：单体 Flask 服务 + 单 Worker 串行执行任务（统一队列消费 UC、百度、夸克、微博任务）。

---

## 三、技术栈

| 层 | 选型 |
|---|---|
| Web 框架 | Flask |
| 数据访问 | SQLAlchemy 2.x（使用 `text(...)` 直接执行原生 SQL） |
| 数据库 | MySQL 8.x |
| 队列 | Redis（List 结构 + Worker 互斥锁） |
| 自动化（UC） | Playwright + Chromium |
| 自动化（百度） | requests 直接调用百度版权保护 API |
| 自动化（夸克） | requests 直接调用夸克版权投诉 API |
| 自动化（微博） | requests 直接调用微博版权投诉 API + ddddocr 识别图形验证码 |
| 数据处理 | pandas / openpyxl |
| HTTP 客户端 | requests |
| 配置管理 | python-dotenv 加载项目根目录 `.env` |
| 前端 | 原生 HTML + Bootstrap 5 + Fuse.js（前端模糊搜索） |

---

## 四、项目结构

```text
complaint_form/
├── app.py                          # Flask Web 主程序：路由、入队、数据库操作
├── worker.py                       # Redis 统一任务消费者（UC + 百度 + 夸克）
├── uc_complaint_from_backend.py    # UC Playwright 自动化脚本（subprocess 调用）
├── uc_compare_upload.py            # UC 上传对比工具
├── baidu_complaint_backend.py      # 百度投诉 API 调用脚本（subprocess 调用）
├── quark_routes.py                 # 夸克投诉 Flask Blueprint（路由：验证/模版/提交/状态/导出）
├── quark_complaint_backend.py      # 夸克投诉 API 调用脚本（subprocess 调用）
├── weibo_routes.py                 # 微博投诉 Flask Blueprint（路由：验证/模版/提交/状态/导出）
├── weibo_complaint_backend.py      # 微博投诉 API 调用脚本（含验证码识别+重试，subprocess 调用）
├── requirements.txt                # 运行时依赖清单
├── deploy_update.sh                # 生产环境部署更新脚本
├── .env                            # 数据库 / Redis / 队列等配置（不入库）
├── .gitignore
├── RUN_GUIDE.md                    # 启动说明
├── 业务操作使用说明.md
├── 技术文档.md                     # 本文
├── static/                         # 前端静态资源、作品 / 资料文件目录
│   └── imgs/
│       ├── 剧名/                   # 作品权属证明文件
│       ├── 营业执照/               # 被代理人营业执照
│       └── 授权委托书/             # 授权委托书
├── templates/                      # 各页面模板
│   ├── index.html                  # 首页（投诉平台入口 + UC/百度/夸克/微博状态查询 + 投诉单号查看跳转）
│   ├── accounts.html               # 投诉账号管理
│   ├── principals.html             # 被代理人信息管理
│   ├── works.html                  # 作品覆盖列表
│   ├── uc.html                     # UC 投诉页面
│   ├── baidu.html                  # 百度投诉页面
│   ├── quark.html                  # 夸克投诉页面
│   ├── weibo.html                  # 微博投诉页面
│   └── kuake.html                  # 旧夸克占位页（已被 quark.html 取代）
├── uploads/                        # 用户上传：投诉提交资料、自定义模板临时目录
│   ├── uc_submissions/
│   └── baidu_submissions/
└── task_results/                   # 每次任务的结果摘要 + 日志（含 quark_*.log）
```

---

## 五、配置（.env）

| 变量 | 默认值 | 说明 |
|---|---|---|
| `DB_HOST` / `DB_PORT` / `DB_NAME` / `DB_USER` / `DB_PASSWORD` | 127.0.0.1 / 3306 / complaint_form / navicat / navicat123 | MySQL 连接 |
| `DATABASE_URL` | 自动拼装 | 也可整体覆盖 |
| `REDIS_URL` | redis://127.0.0.1:6379/0 | Redis 连接串 |
| `UC_QUEUE_NAME` | uc_complaint_queue | UC 投诉队列名 |
| `UNIFIED_QUEUE_NAME` | complaint_queue | 统一投诉队列名 |
| `UC_WORKER_LOCK_KEY` | uc_complaint_worker_lock | Worker 互斥锁 key |
| `UC_WORKER_LOCK_TTL` | 15 | Worker 互斥锁 TTL（秒） |
| `BAIDU_QUEUE_NAME` | baidu_complaint_queue | 百度投诉队列名 |
| `BAIDU_WORKER_LOCK_KEY` | baidu_complaint_worker_lock | 百度 Worker 锁 key |
| `BAIDU_WORKER_LOCK_TTL` | 15 | 百度 Worker 锁 TTL（秒） |

> `.env` 不入仓库；生产环境单独维护一份并使用强密码。

---

## 六、数据库表概览

数据库：`complaint_form`，字符集 `utf8mb4`。共 8 张表：

### 账号 / 被代理人
| 表名 | 说明 |
|---|---|
| `accounts` | 投诉账号（平台、代理主体、账号、Cookie、用途） |
| `principals` | 被代理人信息（平台、代理主体、账号、被代理人名称、营业执照、授权委托书、授权截止日期） |

### 作品资料
| 表名 | 说明 |
|---|---|
| `works` | 作品覆盖列表（作品名称、代理主体、被代理人、内容类型、投诉类型、证明文件） |

### 投诉数据
| 表名 | 说明 |
|---|---|
| `complaints` | 投诉主记录（一次提交的全量快照，含状态、批次进度、投诉单号），UC / 百度 / 夸克 / 微博按 `platform_code` 区分 |
| `complaint_batches` | 每批 Excel 切片状态及对应投诉单号 |
| `submission_works` | 每次提交中各作品的详细信息（百度、夸克、微博投诉使用，含权属/反馈单号信息） |

### 公共字典
| 表名 | 说明 |
|---|---|
| `platforms` | 平台字典（UC、百度、夸克、微博） |
| `dictionaries` | 通用字典（内容类型、投诉类型等） |

---

## 七、运行所需的组件

正常运行需要 3 个组件：

1. **MySQL**：业务数据库
2. **Redis**：投诉任务队列与 Worker 锁
3. **Playwright + Chrome/Chromium**：UC 和小红书自动化浏览器（百度 / 夸克 / 微博投诉不需要，走 API）
4. **ddddocr**：微博投诉的图形验证码识别（仅微博需要，已在 `requirements.txt` 中）

> 生产环境当前实际使用 `/opt/complaint_form/venv` 中的 Playwright Python 包，并通过 `.env` 的 `PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome` 驱动系统 Google Chrome；当前线上 Chrome 版本为 `148.0.7778.178`。因此生产环境不依赖 Playwright 自带下载的 Chromium。

外加 2 个进程：

- `app.py`：Web 服务，监听 5001
- `worker.py`：消费 Redis 统一队列，根据任务类型分发到 UC（Playwright + 系统 Chrome）、小红书（Playwright + 系统 Chrome）、百度（API）、夸克（API）或微博（API + 验证码识别）

---

## 八、本地启动方式

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 安装 Playwright 浏览器
playwright install chromium

# 3. 启动 Redis（任选其一）
brew services start redis           # macOS 推荐
redis-server                        # 前台启动

# 4. 启动 Web
python3 app.py                      # 监听 http://127.0.0.1:5001

# 5. 启动 Worker（另开一个终端）
python3 worker.py
```

> Worker 同一时间只能存在一个，第二个进程会因抢不到 Redis 锁直接退出。

---

## 九、HTTP 路由总览

### 页面
| 路径 | 模板 |
|---|---|
| `/` | `index.html`（首页 + UC/百度/夸克/微博状态查询 + 投诉单号查看跳转） |
| `/accounts` | `accounts.html` |
| `/principals` | `principals.html` |
| `/works` | `works.html` |
| `/uc` | `uc.html` |
| `/baidu` | `baidu.html` |
| `/quark` | `quark.html` |
| `/weibo` | `weibo.html` |
| `/kuake` | `kuake.html`（旧占位页，已被 `/quark` 取代） |
| `/uc/task/<task_id>/log` | 任务日志详情 |

> 首页顶部「投诉单号查看」按钮指向外部 **投诉单号回溯采集系统**（`youjianhuisuxitong.yysh.live:5000/sso`），通过 SSO 免登跳转，URL 自动带上当前登录用户的 `token` 与 `username`。

### 投诉账号
| 路径 | 方法 | 说明 |
|---|---|---|
| `/api/platforms` | GET | 平台列表 |
| `/api/accounts/list` | GET | 账号列表（支持 `?platform_code=` 过滤） |
| `/api/accounts/add` | POST | 新增账号 |
| `/api/accounts/update_cookie` | POST | 更新 Cookie |

### 被代理人
| 路径 | 方法 | 说明 |
|---|---|---|
| `/api/principals/list` | GET | 列表 |
| `/api/principals/document` | GET | 单条资料明细 |
| `/api/principals/add` | POST | 新增被代理人 + 上传资料 |

### 作品覆盖
| 路径 | 方法 | 说明 |
|---|---|---|
| `/api/works/list` | GET | 作品列表 |
| `/api/works/principal_options` | GET | 按代理主体过滤的被代理人下拉 |
| `/api/works/content_types` | GET | 内容类型字典 |
| `/api/works/complaint_types` | GET | 投诉类型字典 |
| `/api/works/add` | POST | 新增作品 + 上传资料 |

### UC 投诉
| 路径 | 方法 | 说明 |
|---|---|---|
| `/api/uc/verify_cookie` | POST | Cookie 校验 |
| `/api/check_excel` | POST | Excel 行数与批次预校验 |
| `/api/download_custom_template` | GET | 下载 UC 自定义模板 |
| `/api/upload_custom_template` | POST | 上传 UC 自定义模板（解析多作品） |
| `/api/uc/submit` | POST | UC 投诉提交（落库 + 入队） |
| `/api/uc/task/<task_id>` | GET | 任务详细状态 |
| `/api/uc/status_list` | GET | 首页 UC 状态查询 |
| `/api/uc/export_excel/<submission_id>` | GET | 导出投诉结果 Excel |
| `/api/proof_file/<path>` | GET | 资料文件预览 |
| `/api/custom_template_file/<id>/<path>` | GET | 自定义模板临时文件下载 |

### 百度投诉
| 路径 | 方法 | 说明 |
|---|---|---|
| `/api/baidu/verify_cookie` | POST | 百度 Cookie 校验 |
| `/api/baidu/pre_check` | POST | 预检作品权属状态 |
| `/api/baidu/search_ownership` | POST | 搜索作品权属记录 |
| `/api/baidu/download_template` | GET | 下载百度自定义模板 |
| `/api/baidu/upload_template` | POST | 上传百度自定义模板 |
| `/api/baidu/submit` | POST | 百度投诉提交（落库 + 入队） |
| `/api/baidu/task/<task_id>` | GET | 任务详细状态 |
| `/api/baidu/status_list` | GET | 首页百度状态查询 |
| `/api/baidu/export_excel/<submission_id>` | GET | 导出投诉结果 Excel |

### 夸克投诉
> 由 `quark_routes.py` 中的 `quark_bp` Blueprint 提供，统一前缀 `/api/quark`。

| 路径 | 方法 | 说明 |
|---|---|---|
| `/api/quark/verify_cookie` | POST | 夸克 Cookie 校验 |
| `/api/quark/download_template` | GET | 下载夸克自定义模板（表单内容/批量导入Excel/填写说明三表） |
| `/api/quark/upload_template` | POST | 上传夸克模板（解析多作品 + 匹配作品覆盖与证明文件） |
| `/api/quark/submit` | POST | 夸克投诉提交（防重 + 落库 + 入队） |
| `/api/quark/task/<task_id>` | GET | 任务详细状态 |
| `/api/quark/status_list` | GET | 首页夸克状态查询 |
| `/api/quark/export_excel/<submission_id>` | GET | 导出投诉结果 Excel |

### 其他
| 路径 | 方法 | 说明 |
|---|---|---|
| `/api/worker/queue_status` | GET | Worker 队列状态 |

---

## 十、投诉执行流程

### 小红书投诉（Playwright + 系统 Chrome）
1. 用户提交 → `xiaohongshu_routes.py` 落库 + 入 Redis 统一队列（`platform='xiaohongshu'`）
2. `worker.py` 消费任务 → subprocess 调用 `xiaohongshu_complaint_backend.py`
3. 脚本通过 Playwright 启动系统 Chrome（生产由 `PLAYWRIGHT_CHROMIUM_PATH` 指定），打开小红书投诉页面，复用页面内 `_webmsxyw` 签名环境
4. 在浏览器上下文内执行权利人查询、模板导入、导入结果查询、侵权明细查询和投诉提交，自动生成 `x-s/x-t`
5. 结果写入 `task_results/` 并更新数据库状态；每个作品可按最多 100 条链接拆分批次

> 小红书不能稳定使用普通 `requests` 直接调用投诉接口。缺少浏览器动态签名时，接口可能返回 `success=true` 和 `batchId`，但后续侵权明细为空。生产环境使用 `/opt/complaint_form/venv`、系统 Chrome `/usr/bin/google-chrome`，需在 `.env` 保留 `PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome`。

### UC 投诉（Playwright 浏览器自动化）
1. 用户提交 → `app.py` 落库 + 入 Redis 队列
2. `worker.py` 消费任务 → subprocess 调用 `uc_complaint_from_backend.py`
3. 脚本启动 Chromium → 登录 UC 投诉平台 → 填写表单 → 上传证明文件 → 批量导入 Excel → 提交
4. 多作品场景：每部作品完成后替换证明文件，继续下一部
5. 结果写入 `task_results/` + 更新数据库状态

### 百度投诉（API 调用）
1. 用户提交 → `app.py` 落库 + 入 Redis 队列
2. `worker.py` 消费任务 → subprocess 调用 `baidu_complaint_backend.py`
3. 脚本通过 requests 调用百度版权保护 API → 上传侵权链接 → 提交投诉
4. 结果写入 `task_results/` + 更新数据库状态

> 百度支持多投诉产品（百度网盘 / 百度搜索 / 百度APP / 百家号 / 百度贴吧 / 好看视频等），投诉产品到 `complaint_type` 编码的映射见 `app.py` 的 `BAIDU_COMPLAINT_TYPE_MAP`；投诉产品为「好看视频」时需额外提供「侵权类型」（`BAIDU_INFRINGE_TYPE_MAP`）。

### 夸克投诉（API 调用）
1. 用户提交 → `quark_routes.py` 的 `/api/quark/submit` 落库 + 入 Redis 统一队列（`platform='quark'`）
2. `worker.py` 消费任务 → subprocess 调用 `quark_complaint_backend.py`
3. 脚本通过 requests 调用夸克版权投诉 API（`https://ipp.quark.cn`）→ 按作品分组、按 200 行/批提交侵权链接
4. 结果写入 `task_results/`（`quark_*.log`）+ 更新数据库状态（含每作品 `feedback_numbers`）

> 夸克功能模块、内容类型、投诉大类/类型到编码的映射见 `quark_routes.py` 中的 `QUARK_MODULE_NAME_MAP` / `QUARK_CONTENT_TYPE_NAME_MAP` / `QUARK_TYPE_MAP` / `QUARK_SUB_TYPE_MAP`。模板上传时会按「作品名 + 被代理人 + 代理主体」匹配 `works` 表，并在 `static/imgs/剧名/` 下匹配证明文件、授权委托书、营业执照。

### 微博投诉（API 调用 + 验证码识别）
1. 用户提交 → `weibo_routes.py` 的 `/api/weibo/submit` 落库 + 入 Redis 统一队列（`platform='weibo'`）
2. `worker.py` 消费任务 → subprocess 调用 `weibo_complaint_backend.py`
3. 脚本通过 requests 调用微博版权投诉 API（`https://service.account.weibo.com`）：先上传共享证件（营业执照、授权委托书等）拿 `picid`，再逐作品逐批提交
4. 每次提交前需图形验证码：取验证码图 → `ddddocr` 识别 → 带验证码提交；若返回 `code=25001`（验证码错误）则重取图重试（最多 8 次）
5. 提交成功后抓 `/rights/my` 页面按侵权链接匹配 `rdid` 作为投诉单号
6. 结果写入 `task_results/`（`weibo_*.log`）+ 更新数据库状态（含每作品 `feedback_numbers`）

> 微博 v1 仅支持机构代理场景。权利类型/权利作品类型/权利来源/投诉内容的中文名到编码映射见 `weibo_routes.py` 中的 `WEIBO_RIGHTS_TYPE_MAP` / `WEIBO_CLASS_ID_MAP` / `WEIBO_EMPOWER_TYPE_MAP` / `WEIBO_C_CONTENT_MAP`（当前仅放开著作权类）。
> **每批链接上限按账号动态判定**：上传模板时抓投诉页读 `whitelist` 标志——受限账号每批仅 1 条，普通账号每批 100 条；探测失败时保守降级为 1 条/批。
> 证件材料在 `static/imgs/` 下匹配：营业执照按全称、授权委托书按「被代理人 + 代理机构简称」双重匹配（简称映射见 `WEIBO_AGENT_ORG_SHORT`）；机构联系人身份证正反面暂复用代理机构营业执照图。文件上传须带正确 MIME 及 `ftype` 字段。

---

## 十一、生产部署（Ubuntu 22.04 摘要）

```bash
# 系统包
sudo apt install -y python3.9 python3.9-venv mysql-server redis-server \
    fonts-noto-cjk fonts-wqy-zenhei

# 数据库
sudo mysql -e "CREATE DATABASE complaint_form CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# 项目
git clone <repo> /opt/complaint_form
cd /opt/complaint_form
python3.9 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt   # 含微博验证码识别依赖 ddddocr

# 如果生产环境使用系统 Google Chrome，可不安装 Playwright 自带 Chromium
# 生产 .env：PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome
# 当前线上已验证：Google Chrome 148.0.7778.178
# 仅在没有可用系统 Chrome 时执行：
playwright install chromium
playwright install-deps

# 写生产 .env（不要复用开发版）
# UC / 小红书 Playwright 使用 headless=True
# 用 systemd 托管 app.py 与 worker.py
# 视情况配置 swap、Nginx 反代、定时清理 task_results / uploads
```

> 微博投诉的图形验证码识别依赖 `ddddocr`（已在 `requirements.txt` 中）。生产环境务必确认它装进了 worker 实际使用的 venv，否则微博任务会在验证码环节反复报 `No module named 'ddddocr'` 而失败。`deploy_update.sh` 会自动检测 `venv/` 或 `.venv/` 并补装 Pillow / ddddocr。

部署更新可使用 `deploy_update.sh` 脚本。

---

## 十二、常见运行问题

| 现象 | 优先排查 |
|---|---|
| 页面打不开 | 看 `app.py` 终端日志 / 5001 端口是否被占 |
| 提交后任务不跑 | Redis 是否启动；`worker.py` 是否在跑；锁 key 是否残留 |
| Worker 启动报 `Another Redis worker is already running` | 已有 worker 在跑或锁未释放，等 TTL 过期或 `redis-cli del uc_complaint_worker_lock` |
| UC 任务失败 | 在首页 UC 状态查询点"查看日志"，定位是 Cookie / 资料 / 平台变化哪类 |
| 百度任务失败 | 在首页百度状态查询点"查看日志"，常见原因：Cookie 过期、权属未通过、API 限流 |
| 夸克任务失败 | 在首页夸克状态查询点"查看日志"（`task_results/quark_*.log`），常见原因：Cookie 过期、作品未覆盖/缺证明文件、API 限流 |
| 微博任务失败 | 在首页微博状态查询点"查看日志"（`task_results/weibo_*.log`），常见原因：Cookie 过期、账号未完成主体认证（返回"请授权认证信息后再提交投诉"）、验证码识别重试耗尽、缺证明文件/授权委托书；`No module named 'ddddocr'` 说明验证码识别依赖未安装，需 `pip install ddddocr` |
| 投诉单号没出来 | 检查作品名称、提交时间窗内平台是否真的入库 |
| 首页状态查询报错 | 检查数据库连接是否正常 |

---

## 十三、版本控制要点

- `.gitignore` 已包含：`.idea/`、`__pycache__/`、`.pytest_cache/`、`uploads/`、`task_results/`、`.DS_Store`
- `.env` 不入仓库，生产环境单独维护
- 实现细节、设计动机、迁移历史等内容请放在单独的开发补充说明里，不要进本文档
