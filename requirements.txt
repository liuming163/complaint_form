# 运行时依赖
# 安装：pip install -r requirements.txt
# Playwright 还需要安装浏览器：playwright install chromium

flask>=2.3
sqlalchemy>=2.0
pymysql>=1.0
redis>=4.5
requests>=2.25
pandas>=2.0
openpyxl>=3.1
playwright>=1.40
python-dotenv>=1.0
werkzeug>=2.3

pycryptodome>=3.18

# 被代理人/作品上传文件超5MB时自动压缩（图片）；PDF压缩另需系统安装 ghostscript
Pillow>=10.0

# 微博投诉验证码识别（onnxruntime 由其自动带入）
ddddocr>=1.4

# 监控/调试用，可选
psutil>=5.9
